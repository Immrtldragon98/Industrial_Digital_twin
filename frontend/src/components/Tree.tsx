import type {TreeNode} from '../types/domain'; function Node({node,depth}:{node:TreeNode;depth:number}){return <div><div className="tree-row" style={{paddingLeft:12+depth*20}}><span className="badge">{node.type==='equipment'?'EQ':'FL'}</span><span>{node.code||'—'}</span><strong>{node.name}</strong>{node.status&&<span className="status">{node.status}</span>}</div>{node.children.map(c=><Node key={c.id} node={c} depth={depth+1}/>)}</div>} export function Tree({nodes}:{nodes:TreeNode[]}){return <div className="tree">{nodes.map(n=><Node key={n.id} node={n} depth={0}/>)}</div>}