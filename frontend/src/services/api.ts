import axios from 'axios'; import type {Equipment,Summary,TreeNode,Twin,RagAnswer} from '../types/domain'; export const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://localhost:8000/api/v1'}); export async function getSummary(){return (await api.get<Summary>('/reliability/summary')).data} export async function getEquipment(){return (await api.get<Equipment[]>('/equipment')).data} export async function getTree(){return (await api.get<TreeNode[]>('/equipment/tree')).data} export async function getTwin(id:string){return (await api.get<Twin>(`/equipment/${id}/twin`)).data} export async function uploadData(file:File,type:string){const f=new FormData();f.append('file',file);return (await api.post(type==='hierarchy'?'/imports/hierarchy':`/imports/sap/${type}`,f)).data} export async function uploadKnowledge(file:File,type:string,equipmentNumber?:string){const f=new FormData();f.append('file',file);f.append('document_type',type);if(equipmentNumber)f.append('equipment_number',equipmentNumber);return(await api.post('/knowledge/documents',f)).data} export async function askKnowledge(question:string,equipmentId?:string){return(await api.post<RagAnswer>('/knowledge/ask',{question,equipment_id:equipmentId||null})).data}
